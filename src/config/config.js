const USER_POOL_ID = "";
const CLIENT_ID = "";
const CLIENT_SECRET = "";
const REGION = "";
//NAME is the name if you want to show in the authentication app who generate the MFA code
const ISSUER_NAME = "";
const USER_EMAIL = "";

module.exports = {
  USER_POOL_ID,
  REGION,
  CLIENT_ID,
  ISSUER_NAME,
  CLIENT_SECRET,
  USER_EMAIL,
};
